@using System.Data;
@using Maxim.Model.BaseModel
@model OrderTracking
@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>_ViewDetail</title>
</head>
<body>
    <div> 
        @*<div class="row-fluid">
            <p class="select" style="font-size: 15px;">
               Customer PO# = [ @Model.Custorderno ],  Maxim# = [ @Model.oos_orderno ].
            </p>
            <div class="clearfix rov">
                @if (!Model.State)
                {
                    <ul class="fl main_left">
                        <li>
                            <span>Print Shop</span>:
                            @Model.Printshop
                        </li>
                        <li>
                            <span>Bill To</span>:
                            @Model.billto
                        </li>
                        <li>
                            <span>Billing Address </span>:
                            @Model.billingaddress
                        </li>
                        <li>
                            <span>Contact </span>:
                            @Model.billcontact
                        </li>
                        <li>
                            <span>Tel</span>:
                            @Model.billcontacttel
                        </li>
                        <li>
                            <span>Order No.</span>:
                            @Model.Custorderno
                        </li>
                        <li>
                            <span>Add Comments </span>:
                            @Model.oos_notes
                        </li>
                    </ul>
                    <ul class="fl main_right">
                        <li>
                            <span>Currency </span>:
                            @Model.Currency
                        </li>
                        <li>
                            <span>Ship To</span>:
                            @Model.shipto
                        </li>
                        <li>
                            <span>Shipping Address</span>:
                            @Model.shippingaddress
                        </li>
                        <li>
                            <span>Contact </span>:
                            @Model.shipcontact
                        </li>
                        <li>
                            <span>Tel</span>:
                            @Model.shipcontacttel
                        </li>
                        <li>
                            <span>Country</span>:
                            @Model.country
                        </li>
                        <li>
                            <span>Shipment Type</span>:
                            @Model.shipmenttype
                        </li>
                    </ul>
                }
            </div>
        </div>*@
        @*<div id="Gridview" class="dataTables_wrapper form-inline" role="grid">
            <table class="table table-striped table-bordered table-hover table-full-width dataTable" aria-describedby="sample_2_info">
                @{
                    var data = Model.OrderDetails as DataTable;
                    var totalqty = 0;
                    <tr>
                        @for (int i = 0; i < data.Columns.Count; i++)
                        {
                            <th>@data.Columns[i].ColumnName</th>
                        }
                    </tr>
                    for (int i = 0; i < data.Rows.Count; i++)
                    {
                        if (Model.TotalQtyColumnName != null && data.Columns.Contains(Model.TotalQtyColumnName))
                        {
                            totalqty += int.Parse(data.Rows[i][Model.TotalQtyColumnName].ToString());
                        }
                        <tr>
                            @for (int j = 0; j < data.Columns.Count; j++)
                            {
                                <td class="center">@data.Rows[i][j]</td>
                            }
                        </tr>
                    }
                    <tr>
                        <td>Totals:</td>
                        <td></td>
                        <td>@totalqty</td>
                    </tr>
                }
            </table>
        </div>*@      

    </div>
</body>
</html>
